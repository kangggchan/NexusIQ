name: Deploy Backend

on:
  push:
    branches:
      - deploy
    paths:
      - 'backend/**'
      - 'services/**'
      - 'retrieval/**'
      - '.github/workflows/backend.yml'
      - 'deploy/k8s/backend-deployment.yaml'
      - 'deploy/k8s/backend-service.yaml'

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  GKE_CLUSTER: ${{ secrets.GKE_CLUSTER }}
  GKE_REGION: ${{ secrets.GKE_REGION }}
  IMAGE_NAME: backend
  REPOSITORY: graphrag-repo

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.GOOGLE_CLOUD_SA_KEY }}'

      - name: Setup Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker
        run: |
          gcloud auth configure-docker asia-northeast3-docker.pkg.dev --quiet

      - name: Build Backend Image
        run: |
          docker build \
            -t asia-northeast3-docker.pkg.dev/$PROJECT_ID/$REPOSITORY/$IMAGE_NAME:${{ github.sha }} \
            ./backend

      - name: Push Backend Image
        run: |
          docker push \
            asia-northeast3-docker.pkg.dev/$PROJECT_ID/$REPOSITORY/$IMAGE_NAME:${{ github.sha }}

      - name: Connect to GKE
        run: |
          gcloud container clusters get-credentials \
          $GKE_CLUSTER \
          --region $GKE_REGION

      - name: Update Backend Deployment
        run: |
          kubectl set image deployment/backend \
          backend=asia-northeast3-docker.pkg.dev/$PROJECT_ID/$REPOSITORY/$IMAGE_NAME:${{ github.sha }}

      - name: Wait for Rollout
        run: |
          kubectl rollout status deployment/backend --timeout=300s